/** Scopes pushed via megacompanion_request_force_sync — safe for client + server. */
export const FORCE_SYNC_ALL_SCOPES = [
  "watchlist",
  "catalogs",
  "iptv",
  "settings",
  "addons",
  "devices"
] as const;
