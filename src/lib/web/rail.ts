/** Max posters visible in a horizontal home rail before "Voir tout". */
export const RAIL_PREVIEW_LIMIT = 12;

/** Scroll step when using rail arrow buttons (px). */
export const RAIL_SCROLL_STEP = 520;
