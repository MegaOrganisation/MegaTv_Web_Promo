/** Max posters visible in a horizontal home rail before "Voir tout". */
export const RAIL_PREVIEW_LIMIT = 20;

/** Max items fetched from catalog preview API for see-all modal. */
export const RAIL_FETCH_LIMIT = 60;

/** Scroll step when using rail arrow buttons (px). */
export const RAIL_SCROLL_STEP = 780;
