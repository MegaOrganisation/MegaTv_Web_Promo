"use client";

/**
 * Scène ambient — grain léger statique (pas data-dynamic → pas de re-capture WebGL).
 */
export function CompanionSettingsScene() {
  return <div className="companion-lg-scene-grain" aria-hidden="true" />;
}
